<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Chi tiết sách</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link href="/css/NavBar.css" rel="stylesheet">
</head>
<body>  
    <div class="sidebar">
        <a href="@Url.RouteUrl("Default")">📚 Thư viện</a>
        <a href="@Url.RouteUrl("Default")">Tìm kiếm sách</a>
        <a href="@Url.RouteUrl("DanhSachMuon")">Danh sách mượn</a>
        <a href="@Url.RouteUrl("MuonSach")">Yêu cầu mượn</a>
        <a href="#" class="btn btn-light text-primary mt-3">Đăng nhập</a>
    </div>
            @RenderBody()
</body>
</html>
